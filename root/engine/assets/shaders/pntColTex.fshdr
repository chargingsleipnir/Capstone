precision mediump float;

varying vec3 v_Col;

uniform sampler2D u_Sampler;

void main()
{
	vec4 texColour = texture2D(u_Sampler, gl_PointCoord);
	gl_FragColor = vec4(v_Col + texColour.rgb, texColour.a);
}