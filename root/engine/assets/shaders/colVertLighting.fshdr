precision mediump float;

varying vec3 v_LightWeighting;
varying vec3 v_Color;

uniform vec3 u_ColourTint;
uniform float u_Alpha;

void main()
{
	gl_FragColor = vec4((v_Color * v_LightWeighting) + u_ColourTint, u_Alpha);
}